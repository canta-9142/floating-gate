# Floating-Gate.com デザインシステム

この文書は、現時点のサイト実装から読み取れるデザイン上の約束をまとめたものです。新しいページやコンポーネントを追加するときは、ここにあるトークン、余白、情報の優先順位を基準にして、既存ページと自然につながる見た目にします。

対象は `src/` 配下で提供する Floating-Gate.com 本体です。`public/projects/*/app/` に置かれた個別制作物のアプリケーションは、それぞれ独立した UI であり、このシステムの対象外です。

## デザインの方向性

- **静かで技術的**: 装飾は最小限にし、本文・タイトル・メタデータの階層で内容を読ませる。
- **やわらかい無彩色**: 純白ではない背景と、ごく淡い罫線で画面を区切る。
- **ティールを意味のある箇所だけに使う**: リンク、現在地、進行中の状態、キーボードフォーカスを一貫して示す。
- **コンテンツを中心に置く**: 長文は読みやすい狭い幅に保ち、一覧やトップだけ必要に応じて広いコンテナを使う。
- **ライト／ダークの両対応**: OS のカラースキームに追従し、色の役割を CSS 変数で切り替える。

## 基本トークン

### 色

色は用途名で参照する。新しい色を直接書く前に、既存トークンで表せないか検討する。

| 用途 | ライト | ダーク | 実装トークン |
| --- | --- | --- | --- |
| ページ背景 | `#f8f8f6` | `#171a1b` | `--background` / `bg-site-background` |
| 面（カード・画像背景） | `#ffffff` | `#1d2122` | `--surface` / `bg-site-surface` |
| 基本文字 | `#272b2d` | `#e4e7e5` | `--text` / `text-site-text` |
| 補足文字 | `#626a6d` | `#a7afad` | `--muted` / `text-site-muted` |
| 罫線 | `#d9dddb` | `#39403f` | `--faint` / `border-site-faint` |
| アクセント | `#276c65` | `#76bdb3` | `--accent` / `text-site-accent` |
| 強いアクセント・リンク | `#174f4a` | `#a4d6cf` | `--accent-strong` / `text-site-accent-strong` |
| アクセントの淡色面 | `#e2efec` | `#233a37` | `--accent-soft` / `bg-site-accent-soft` |
| インラインコード／コードブロック | `#f0f1ee` | `#222728` | `--code` / `bg-site-code` |

### 書体と文字組み

- 基本文字は `'Hiragino Sans', 'Yu Gothic', 'Noto Sans JP', Meiryo, sans-serif`。本文の標準は `16px`、行高は `1.75`。
- 本文はやや大きい `1.02rem`、導入文は `1.05–1.075rem` とし、本文より少しだけ存在感を上げる。
- 見出しはすべて `font-semibold` を基本とする。太いウェイトや派手な書体は追加しない。
- 大見出しは `clamp(2rem, 6vw, 3rem)`（ホームだけ最大 `3.25rem`）、行高 `1.1–1.15`。詳細ページでは `letter-spacing: -0.035em`、ホームのサイト名は `0.015em`。
- 一覧タイトルは `1.25rem`、本文中の `h2` は `1.5rem`、`h3` は `1.25rem`。メタデータ・タグ・フッターはおおむね `0.82rem`、ラベルは `0.75rem` を用いる。

### 幅と余白

| 名称 | 値 | 用途 |
| --- | --- | --- |
| `--measure` | `46rem` | 記事、固定ページ、一覧、ヒーローの読み幅 |
| `--wide` | `68rem` | ヘッダー、フッター、ページ全体、ホームの2カラム |
| ページ内側余白 | `1rem` → `1.5rem`（`sm`以上）→ `0`（`xl`以上） | 幅広コンテナ内の左右余白 |
| メイン上下余白 | `2.5rem` → `4rem`（`sm`）→ `5rem`（`lg`） | ページ本文の開始・終了位置 |
| ページ見出し下 | `3rem` | ヒーローとコンテンツの区切り |
| セクション区切り | `4rem` | 関連コンテンツなど大きな区切り |

余白は細かく詰めすぎず、一覧項目には上下 `1.5rem`、カード内には `0.75–1rem`、見出し下には `0.5–1rem` 程度の呼吸を作る。

## レイアウト

### 共通フレーム

- ヘッダーとフッターは `--wide` の中央コンテナに置き、ページ上下を 1px の `--faint` で区切る。
- ヘッダーは最低高 `4.5rem`。サイト名を左、ナビゲーションを右に置く。幅 `32rem` 未満では縦積みにする。
- メインは最低でも画面高からヘッダー・フッター相当を引いた高さを確保し、フッターが不自然に上がらないようにする。
- 本文、一覧、固定ページの主コンテンツは `--measure` に制限する。幅を広げるのはホームの2カラムなど、比較・一覧性が必要な場面に限る。

### ページ構成

1. ページ上部に、タイトル、短い説明、必要なら状態・メタデータ・タグを置く。
2. タイトルと本文の間には大きめの余白（通常 `3rem`）を取る。
3. 本文の後に関連コンテンツを置く場合は、`4rem` の上余白、上罫線、`1.5rem` の内側上余白で独立させる。
4. ホームは記事を主列、プロフィールと注目制作物を補助列にする。`md` 未満では1列に戻し、補助列は上罫線で区切る。

## コンポーネントの見た目

### リンクとナビゲーション

- 通常リンクは強いアクセント色、下線の太さ `0.08em`、オフセット `0.18em`。ホバー時だけ下線を `0.14em` に太くする。
- 記事・制作物タイトルは基本文字色で表示し、ホバー時に強いアクセント色と下線を加える。初期状態で本文をリンクだらけに見せないためである。
- 現在のナビゲーション項目は基本文字色・中程度のウェイト・アクセント色の 2px 下線で示す。下線オフセットは大きめ（`0.4rem`）。
- キーボードフォーカスはアクセント色の `3px` アウトラインと `3px` オフセットで、背景色の変化に頼らず明確に示す。

### リストとメタデータ

- 記事・制作物の一覧項目はカード化せず、上下余白で区切るフラットな構成にする。
- 順序は「状態または種別 → タイトル → 説明 → タグ／技術」。補足情報は `--muted`、小さめの文字で主情報を邪魔しないようにする。
- 記事種別は強いアクセント色の太字。タグは `#` をアクセント色、本文を補足色で表示する。
- 制作物の状態は小さな円とラベルで表す。進行中はアクセント色の輪郭、完了は補足色の塗りつぶし円を使う。色だけで状態を伝えない。

### カード、画像、外部リンク

- 画像付き外部リンクだけを、`--surface` 背景・1px 罫線・角丸なしのカードとして扱う。ホバー時は罫線をアクセント色に変える。
- カード画像は下罫線で本文から分離し、カード本文は `1rem` の内側余白と太字ラベルを使う。
- 制作物のカバー画像はコンテンツ幅いっぱい、`--surface` 背景、1px 罫線。角丸や強い影は使わない。
- 外部リンクには `↗` とスクリーンリーダー用の「新しいタブで開く」案内を添える。

### 本文（Markdown）

- `h2` は上余白 `3.25rem`、下余白 `1rem`、下罫線、下内側余白 `0.375rem`。章の切れ目を明快にする。
- `h3` は上余白 `2.5rem`、下余白 `0.75rem`。`h4` も同じ控えめな字間・行高にする。
- 段落、リスト、引用は上下 `1.25rem`。リストの左余白は `1.5rem`。
- インラインコードは淡いコード背景、控えめな `2px` 角丸、左右 `0.375rem`・上下 `0.125rem` の余白。
- コードブロックは淡いコード背景、1px 罫線、控えめな `2px` 角丸、`1rem` の内側余白。横にはみ出す場合は横スクロールさせる。
- 引用は左に 3px のアクセント罫線、左内側余白 `1rem`、補足色の文字。表は罫線付きで横スクロール可能にする。
- 図版は上下 `2rem`、キャプションは中央寄せ・小さめ・補足色。

## レスポンシブとモーション

- モバイルを基準に、横方向の余白とコンテンツ幅を確保する。本文の最小幅は `18rem`。
- ヘッダーは `32rem` 未満で縦積み、ホームの2カラムは `md` 未満で1列、制作物の詳細情報は `45rem` 未満で1列にする。
- タイトルは `clamp()` で縮小し、画面幅に応じて読みやすい大きさを保つ。
- 動きは必須の表現にしない。`prefers-reduced-motion: reduce` ではスクロール、アニメーション、トランジションを実質停止する。

## 実装上のガイド

- Tailwind の用途名ユーティリティ（例: `text-site-muted`、`border-site-faint`）を優先し、同じ役割の HEX 値をコンポーネントごとに増やさない。
- 角丸、ドロップシャドウ、グラデーション、色面の多用は現行トーンから外れるため、明確な情報上の理由がない限り追加しない。
- 新しい UI はまずフラットな本文・罫線・余白だけで構成し、カード化は「画像を伴う外部導線」のように境界が必要な場合だけにする。
- 本文画像には代替テキストを付ける。装飾画像は空の `alt` にする。リンクのホバー色に依存せず、フォーカス状態と現在地を必ず用意する。
- 色の正確な定義と Markdown 本文の細部は [`src/styles/global.css`](src/styles/global.css) を正とする。共通フレームは [`src/layouts/Layout.astro`](src/layouts/Layout.astro)、代表的なコンポーネントは `src/components/` を参照する。
