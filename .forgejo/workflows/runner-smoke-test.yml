name: Runner smoke test
on:
  push:
    branches:
      - main
    workflow_dispatch:
  jobs:
    smoke-test:
      runs-on: node-22
      steps:
        - name: Show environment
          run: |
            echo "Forgejo Runner is working"
            node --version
            npm --version
            uname -m
            cat /etc/os-release
