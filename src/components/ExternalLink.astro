---
interface Props {
	href: string;
}

const { href } = Astro.props;
---

<a href={href} target="_blank" rel="noreferrer">
	<slot />
	<span class="ml-0.5 inline-block text-xs" aria-hidden="true">↗</span>
	<span class="sr-only">（外部サイト、新しいタブで開きます）</span>
</a>
